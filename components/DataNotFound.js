import React from 'react';

const DataNotFound = ({element}) => {
    return (
        <div className={'flex justify-center items-center w-full h-full'}>
            {element}
        </div>
    );
};

export default DataNotFound;